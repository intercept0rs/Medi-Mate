import React from 'react';

/**
 * @deprecated This component is redundant and has been replaced by WelcomeScreen.tsx.
 * It is kept for reference and should be removed in a future cleanup.
 * The authentication flow has been consolidated to use WelcomeScreen as the single entry point.
 */
const AuthScreen: React.FC = () => {
  return null;
};

export default AuthScreen;
